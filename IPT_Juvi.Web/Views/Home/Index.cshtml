@{
    ViewData["Title"] = "Home";
    ViewData["Nav"] = "Site";
}

<div class="bizzy-shell">
    <div class="bizzy-card">
        <div class="bizzy-panel-title">HOME</div>
        <div class="bizzy-panel-hr"></div>
        <div style="font-weight:800;font-size:12px;color:rgba(0,0,0,.75);">
            Use <a asp-controller="Account" asp-action="Profile">PROFILE</a> and <a asp-controller="Account" asp-action="Settings">ACCOUNT SETTINGS</a> to view the pages converted from your images.
        </div>
    </div>
</div>

